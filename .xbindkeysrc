# This bind keys volume keys in the Lenovo T400 to
# alsamixer commands and such

# Default 'show' combination
"xbindkeys_show"
  control+shift + q

# Use the Blue 'ThinkVantage' button as a showtcut
# to lock the screen.
"xtrlock"
  XF86Launch1

# Or the Calculator key (fn + y in the
"xscreensaver-command -lock"
  XF86AudioStop

# Raise volume by 22% when pressing the volup key
"amixer set Master 5+"
  XF86AudioRaiseVolume

# Lower main mixer volume by 5%
"amixer set Master 5-"
  XF86AudioLowerVolume

# Mute
"amixer set Master toggle"
  XF86AudioMute

